# Project Chimera — frontend env template
# Copy to .env. This is the only env var the frontend needs.
# All secrets (Mailgun API key, encryption key, cron secret) live in
# Supabase Vault and are only readable by the edge functions.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
